# Minimal Vault Example

Install only `templates/core/`.

Suggested first session:

```txt
run
```

Then:

```txt
condense
handoff
clear-handoff
```

This profile is enough to test whether the memory and handoff model fits your
workflow before adopting research or maintenance packs.
